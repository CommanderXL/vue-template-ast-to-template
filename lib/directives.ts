enum DIRECTIVES {
  text = 'v-text',
  html = 'v-html',
  show = 'v-show',
  if = 'v-if',
  else = 'v-else',
  elseif = 'v-else-if',
  for = 'v-for',
  on = 'v-on',
  bind = 'v-bind',
  model = 'v-model',
  slot = 'v-slot',
  pre = 'v-pre',
  cloak = 'v-cloak',
  once = 'v-once'
}

export default DIRECTIVES